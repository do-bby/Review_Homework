# Review_Homework
Framework : Spring
DB :MySQL

테이블과 인덱스에 대한 DDL schema.sql파일 작성

애플리케이션 실행 후 postman으로 api테스트

reviewService에 대한 test case reviewServiceTest로 테스트

POST /events 리뷰이벤트 발생

GET /points/{memberId} 회원 포인트조회

GET /events/{reviewId} 리뷰 조회

DELETE /events/{reviewId} 리뷰 삭제

PATCH /events/{reviewId} 리뷰 수정

